"Above200" = "Cell Count")) %>%
add_overall()
t1
#|label: table-study-pop
#|tbl-cap: "Table 1: Study Population"
t1 <- f6 %>%
dplyr::select(AGE, Gender, Race, Above200, Randgrp) %>%
tbl_summary(by = Randgrp,
label = list("AGE" = "Age",
"Above200" = "Cell Count")) %>%
add_overall() %>%
modify_caption("Table 1: Study Population")
t1
#|label: table-study-pop
#|tbl-cap: "Table 1: Study Population"
t1 <- f6 %>%
dplyr::select(AGE, Gender, Race, Above200, Randgrp) %>%
tbl_summary(by = Randgrp,
label = list("AGE" = "Age",
"Above200" = "Cell Count")) %>%
add_overall() %>%
modify_caption("**Table 1: Study Population**")
t1
#| code-fold: true
#| code-summary: "Click here to view table 1 export"
t1 %>%
as_flex_table() %>%
save_as_docx(path = "Exports/Table_1")
# Chunk 1
#| code-fold: true
#| code-summary: "Click here to view loaded libraries"
#| message: false
#| warning: false
library(tidyverse)
library(survival)
library(ggfortify) ## to calculate "Schoenfeld residuals vs time"
library(survminer)
library(multcomp)
library(emmeans) ## to calculate hazard ratios
library(gtsummary) ## to make table
library(flextable) ## export table 1 to word
# Chunk 2
f6 <- read.csv("first6.csv")
# Chunk 3
#| code-fold: true
#| code-summary: "Click here to view data cleaning code"
f6 <- f6 %>%
mutate(Gender = as.factor(if_else(GENDER == 1, "Male", "Female", missing = NULL)),
Race = as.factor(case_when(RACE == 1 ~ "Latino",
RACE == 2 ~ "African American",
RACE == 3 ~ "White",
RACE == 4 ~ "Other")),
Above200 = as.factor(if_else(above200 == 1, "Above 200 cell count", "Below 200 cell count")),
Randgrp = as.factor(case_when(RANDGRP == 1 ~ "PI + NRTI",
RANDGRP == 2 ~ "NNRTI + NRTI",
RANDGRP == 3 ~ "PI + NNRTI + NRTI")))
# Chunk 4: table-study-pop
#|label: table-study-pop
#|tbl-cap: "Table 1: Study Population"
t1 <- f6 %>%
dplyr::select(AGE, Gender, Race, Above200, Randgrp) %>%
tbl_summary(by = Randgrp,
label = list("AGE" = "Age",
"Above200" = "Cell Count")) %>%
add_overall() %>%
modify_caption("**Table 1: Study Population**")
t1
# Chunk 5: export to word
#| code-fold: true
#| code-summary: "Click here to view table 1 export"
t1 %>%
as_flex_table() %>%
save_as_docx(path = "Exports/Table_1")
#| code-fold: true
#| code-summary: "Click here to view table 1 export"
t1 %>%
as_flex_table() %>%
save_as_docx(path = "Exports/Table_1.docx")
#| code-fold: true
#| code-summary: "Click here to view data cleaning code"
f6 <- f6 %>%
mutate(Gender = as.factor(if_else(GENDER == 1, "Male", "Female", missing = NULL)),
Race = as.factor(case_when(RACE == 1 ~ "Latino",
RACE == 2 ~ "African American",
RACE == 3 ~ "White",
RACE == 4 ~ "Other")),
Above200 = as.factor(if_else(above200 == 1, "Above 200 cell count", "Below 200 cell count")),
Randgrp = as.factor(case_when(RANDGRP == 1 ~ "PI + NRTI",
RANDGRP == 2 ~ "NNRTI + NRTI",
RANDGRP == 3 ~ "PI + NNRTI + NRTI")),
twoclass = as.factor(twoclass))
AIDS_fit <- survfit(Surv(t2qoi, qoi) ~ twoclass, data = f6)
#| label: kaplan-meier-plot
#| fig-cap: "Kaplan-Meier: AIDS Survival Analysis"
autoplot(AIDS_fit) +
xlab("ASD") +
ylab("asd") +
labs(title = "asds")
#| label: kaplan-meier-plot
#| fig-cap: "Kaplan-Meier: AIDS Survival Analysis"
autoplot(AIDS_fit) +
xlab("Time (Months)") +
ylab("AIDS Survival Percentage") +
labs(title = "Time to AIDS",
subtitle = "By Treatment Type") +
theme_bw()
#| code-fold: true
#| code-summary: "Click here to view data cleaning code"
f6 <- f6 %>%
mutate(Gender = as.factor(if_else(GENDER == 1, "Male", "Female", missing = NULL)),
Race = as.factor(case_when(RACE == 1 ~ "Latino",
RACE == 2 ~ "African American",
RACE == 3 ~ "White",
RACE == 4 ~ "Other")),
Above200 = as.factor(if_else(above200 == 1, "Above 200 cell count", "Below 200 cell count")),
Randgrp = as.factor(case_when(RANDGRP == 1 ~ "PI + NRTI",
RANDGRP == 2 ~ "NNRTI + NRTI",
RANDGRP == 3 ~ "PI + NNRTI + NRTI")),
twoclass = as.factor(twoclass),
TwoClass = as.factor(case_when(twoclass == 1 ~ "Two Class",
twoclass == 2 ~ "Three Class")))
AIDS_fit <- survfit(Surv(t2qoi, qoi) ~ TwoClass, data = f6)
#| label: kaplan-meier-plot
#| fig-cap: "Kaplan-Meier: AIDS Survival Analysis"
autoplot(AIDS_fit) +
xlab("Time (Months)") +
ylab("AIDS Survival Percentage") +
labs(title = "Time to AIDS",
subtitle = "By Treatment Type") +
theme_bw()
#| label: kaplan-meier-plot
#| fig-cap: "Kaplan-Meier: AIDS Survival Analysis"
autoplot(AIDS_fit) +
xlab("Time (Months)") +
ylab("AIDS Survival Percentage") +
labs(title = "Time to AIDS",
subtitle = "By Treatment Type") +
theme_bw() +
scale_color_discrete(name = "Treatment")
#| label: kaplan-meier-plot
#| fig-cap: "Kaplan-Meier: AIDS Survival Analysis"
autoplot(AIDS_fit) +
xlab("Time (Months)") +
ylab("AIDS Survival Percentage") +
labs(title = "Time to AIDS",
subtitle = "By Treatment Type") +
theme_minimal()
# Chunk 1
#| code-fold: true
#| code-summary: "Click here to view loaded libraries"
#| message: false
#| warning: false
library(tidyverse)
library(survival)
library(ggfortify) ## to calculate "Schoenfeld residuals vs time"
library(survminer)
library(multcomp)
library(emmeans) ## to calculate hazard ratios
library(gtsummary) ## to make table
library(flextable) ## export tables to Word
# Chunk 2
f6 <- read.csv("first6.csv")
# Chunk 3
#| code-fold: true
#| code-summary: "Click here to view data cleaning code"
f6 <- f6 %>%
mutate(Gender = as.factor(if_else(GENDER == 1, "Male", "Female", missing = NULL)),
Race = as.factor(case_when(RACE == 1 ~ "Latino",
RACE == 2 ~ "African American",
RACE == 3 ~ "White",
RACE == 4 ~ "Other")),
Above200 = as.factor(if_else(above200 == 1, "Above 200 cell count", "Below 200 cell count")),
Randgrp = as.factor(case_when(RANDGRP == 1 ~ "PI + NRTI",
RANDGRP == 2 ~ "NNRTI + NRTI",
RANDGRP == 3 ~ "PI + NNRTI + NRTI")),
twoclass = as.factor(twoclass),
TwoClass = as.factor(
fct_relevel(case_when(twoclass == 1 ~ "Two Class",
twoclass == 2 ~ "Three Class"),"Two Class"))) ## adjust Two Class as reference level
# Chunk 4: tbl-study-pop
#| label: tbl-study-pop
#| tbl-cap: "Study Population Characteristics"
#| tbl-cap-location: top
t1 <- f6 %>%
dplyr::select(AGE, Gender, Race, Above200, Randgrp) %>%
tbl_summary(by = Randgrp,
label = list("AGE" = "Age",
"Above200" = "Cell Count")) %>%
add_overall() %>%
as_flex_table()
t1
# Chunk 6: km model
AIDS_fit <- survfit(Surv(t2qoi, qoi) ~ TwoClass, data = f6)
# Chunk 7: fig-kmplot
#| label: fig-kmplot
#| fig-cap: "Kaplan-Meier: AIDS Survival Analysis"
#| warning: false
autoplot(AIDS_fit) +
xlab("Time (Months)") +
ylab("AIDS Survival Percentage") +
labs(title = "Time to AIDS",
subtitle = "By Treatment Type") +
theme_minimal()
# Chunk 8
AIDS_model <- coxph(Surv(t2qoi, qoi) ~ TwoClass + UNIT + Above200, data = f6)
summary(AIDS_model)
# Chunk 10: tbl-AIDS
#| label: tbl-AIDS
#| tbl-cap: "Multiple Cox Proportional Hazards Regression"
#| tbl-cap-location: top
AIDS_table <- tbl_regression(AIDS_model, exponentiate = TRUE) %>%
bold_labels() %>%
italicize_levels() %>%
as_flex_table()  ## this is so I can copy-paste directly from the html render
AIDS_table ## turns out I can copy-paste directly from the html render!!!
# Chunk 11
AIDS_model_unadj <- coxph(Surv(t2qoi, qoi) ~ TwoClass, data = f6)
summary(AIDS_model_unadj)
# Chunk 12: fig-schoenfeld
#| label: fig-schoenfeld
#| fig-cap: " Schoenfeld Residuals vs Time Plot"
#| fig-cap-location: top
AIDS_resid <- cox.zph(AIDS_model_unadj)
ggcoxzph(AIDS_resid)
AIDS_model <- coxph(Surv(t2qoi, qoi) ~ TwoClass + UNIT + Above200, data = f6)
summary(AIDS_model)
View(f6)
# Chunk 1
#| code-fold: true
#| code-summary: "Click here to view loaded libraries"
#| message: false
#| warning: false
library(tidyverse)
library(survival)
library(ggfortify) ## to calculate "Schoenfeld residuals vs time"
library(survminer)
library(multcomp)
library(emmeans) ## to calculate hazard ratios
library(gtsummary) ## to make table
library(flextable) ## export tables to Word
# Chunk 2
f6 <- read.csv("first6.csv")
# Chunk 3
#| code-fold: true
#| code-summary: "Click here to view data cleaning code"
f6 <- f6 %>%
mutate(Gender = as.factor(if_else(GENDER == 1, "Male", "Female", missing = NULL)),
Race = as.factor(case_when(RACE == 1 ~ "Latino",
RACE == 2 ~ "African American",
RACE == 3 ~ "White",
RACE == 4 ~ "Other")),
Above200 = as.factor(if_else(above200 == 1, "Above 200 cell count", "Below 200 cell count")),
Randgrp = as.factor(case_when(RANDGRP == 1 ~ "PI + NRTI",
RANDGRP == 2 ~ "NNRTI + NRTI",
RANDGRP == 3 ~ "PI + NNRTI + NRTI")),
twoclass = as.factor(twoclass),
TwoClass = as.factor(
fct_relevel(case_when(twoclass == 1 ~ "Two Class",
twoclass == 2 ~ "Three Class"),"Two Class")), ## adjust Two Class as reference level
Unit = as.factor(UNIT)) # make UNIT into factor
# Chunk 4: tbl-study-pop
#| label: tbl-study-pop
#| tbl-cap: "Study Population Characteristics"
#| tbl-cap-location: top
t1 <- f6 %>%
dplyr::select(AGE, Gender, Race, Above200, Randgrp) %>%
tbl_summary(by = Randgrp,
label = list("AGE" = "Age",
"Above200" = "Cell Count")) %>%
add_overall() %>%
as_flex_table()
t1
# Chunk 6: km model
AIDS_fit <- survfit(Surv(t2qoi, qoi) ~ TwoClass, data = f6)
# Chunk 7: fig-kmplot
#| label: fig-kmplot
#| fig-cap: "Kaplan-Meier: AIDS Survival Analysis"
#| warning: false
autoplot(AIDS_fit) +
xlab("Time (Months)") +
ylab("AIDS Survival Percentage") +
labs(title = "Time to AIDS",
subtitle = "By Treatment Type") +
theme_minimal()
# Chunk 8
AIDS_model <- coxph(Surv(t2qoi, qoi) ~ TwoClass + UNIT + Above200, data = f6)
summary(AIDS_model)
# Chunk 10: tbl-AIDS
#| label: tbl-AIDS
#| tbl-cap: "Multiple Cox Proportional Hazards Regression"
#| tbl-cap-location: top
AIDS_table <- tbl_regression(AIDS_model, exponentiate = TRUE) %>%
bold_labels() %>%
italicize_levels() %>%
as_flex_table()  ## this is so I can copy-paste directly from the html render
AIDS_table ## turns out I can copy-paste directly from the html render!!!
# Chunk 11
AIDS_model_unadj <- coxph(Surv(t2qoi, qoi) ~ TwoClass, data = f6)
summary(AIDS_model_unadj)
# Chunk 12: fig-schoenfeld
#| label: fig-schoenfeld
#| fig-cap: " Schoenfeld Residuals vs Time Plot"
#| fig-cap-location: top
AIDS_resid <- cox.zph(AIDS_model_unadj)
ggcoxzph(AIDS_resid)
AIDS_model <- coxph(Surv(t2qoi, qoi) ~ TwoClass + Unit + Above200, data = f6)
summary(AIDS_model)
#| label: fig-schoenfeld
#| fig-cap: " Schoenfeld Residuals vs Time Plot"
#| fig-cap-location: top
AIDS_resid <- cox.zph(AIDS_model_unadj)
ggcoxzph(AIDS_resid)
AIDS_model <- coxph(Surv(t2qoi, qoi) ~ TwoClass + Unit + Above200, data = f6)
summary(AIDS_model)
#| label: tbl-AIDS
#| tbl-cap: "Multiple Cox Proportional Hazards Regression"
#| tbl-cap-location: top
AIDS_table <- tbl_regression(AIDS_model, exponentiate = TRUE) %>%
bold_labels() %>%
italicize_levels() %>%
as_flex_table()  ## this is so I can copy-paste directly from the html render
AIDS_table ## turns out I can copy-paste directly from the html render!!!
# Chunk 1
#| code-fold: true
#| code-summary: "Click here to view loaded libraries"
#| message: false
#| warning: false
library(tidyverse)
library(survival)
library(ggfortify) ## to calculate "Schoenfeld residuals vs time"
library(survminer)
library(multcomp)
library(emmeans) ## to calculate hazard ratios
library(gtsummary) ## to make table
library(flextable) ## export tables to Word
# Chunk 2
f6 <- read.csv("first6.csv")
# Chunk 3
#| code-fold: true
#| code-summary: "Click here to view data cleaning code"
f6 <- f6 %>%
mutate(Gender = as.factor(if_else(GENDER == 1, "Male", "Female", missing = NULL)),
Race = as.factor(case_when(RACE == 1 ~ "Latino",
RACE == 2 ~ "African American",
RACE == 3 ~ "White",
RACE == 4 ~ "Other")),
Above200 = as.factor(if_else(above200 == 1, "Above 200 cell count", "Below 200 cell count")),
Randgrp = as.factor(case_when(RANDGRP == 1 ~ "PI + NRTI",
RANDGRP == 2 ~ "NNRTI + NRTI",
RANDGRP == 3 ~ "PI + NNRTI + NRTI")),
twoclass = as.factor(twoclass),
TwoClass = as.factor(
fct_relevel(case_when(twoclass == 1 ~ "Two Class",
twoclass == 2 ~ "Three Class"),"Two Class")), ## adjust Two Class as reference level
Unit = as.factor((UNIT), ref = "Below 200 cell count")) # make UNIT into factor
#| code-fold: true
#| code-summary: "Click here to view data cleaning code"
f6 <- f6 %>%
mutate(Gender = as.factor(if_else(GENDER == 1, "Male", "Female", missing = NULL)),
Race = as.factor(case_when(RACE == 1 ~ "Latino",
RACE == 2 ~ "African American",
RACE == 3 ~ "White",
RACE == 4 ~ "Other")),
Above200 = as.factor(if_else(above200 == 1, "Above 200 cell count", "Below 200 cell count")),
Randgrp = as.factor(case_when(RANDGRP == 1 ~ "PI + NRTI",
RANDGRP == 2 ~ "NNRTI + NRTI",
RANDGRP == 3 ~ "PI + NNRTI + NRTI")),
twoclass = as.factor(twoclass),
TwoClass = as.factor(
fct_relevel(case_when(twoclass == 1 ~ "Two Class",
twoclass == 2 ~ "Three Class"),"Two Class")), ## adjust Two Class as reference level
Unit = relevel(as.factor((UNIT), ref = "Below 200 cell count"))) # make UNIT into factor
# Chunk 1
#| code-fold: true
#| code-summary: "Click here to view loaded libraries"
#| message: false
#| warning: false
library(tidyverse)
library(survival)
library(ggfortify) ## to calculate "Schoenfeld residuals vs time"
library(survminer)
library(multcomp)
library(emmeans) ## to calculate hazard ratios
library(gtsummary) ## to make table
library(flextable) ## export tables to Word
# Chunk 2
f6 <- read.csv("first6.csv")
# Chunk 3
#| code-fold: true
#| code-summary: "Click here to view data cleaning code"
f6 <- f6 %>%
mutate(Gender = as.factor(if_else(GENDER == 1, "Male", "Female", missing = NULL)),
Race = as.factor(case_when(RACE == 1 ~ "Latino",
RACE == 2 ~ "African American",
RACE == 3 ~ "White",
RACE == 4 ~ "Other")),
Above200 = as.factor(if_else(above200 == 1, "Above 200 cell count", "Below 200 cell count")),
Randgrp = as.factor(case_when(RANDGRP == 1 ~ "PI + NRTI",
RANDGRP == 2 ~ "NNRTI + NRTI",
RANDGRP == 3 ~ "PI + NNRTI + NRTI")),
twoclass = as.factor(twoclass),
TwoClass = as.factor(
fct_relevel(case_when(twoclass == 1 ~ "Two Class",
twoclass == 2 ~ "Three Class"),"Two Class")), ## adjust Two Class as reference level
Unit = relevel(as.factor(UNIT, ref = "Below 200 cell count"))) # make UNIT into factor
#| code-fold: true
#| code-summary: "Click here to view data cleaning code"
f6 <- f6 %>%
mutate(Gender = as.factor(if_else(GENDER == 1, "Male", "Female", missing = NULL)),
Race = as.factor(case_when(RACE == 1 ~ "Latino",
RACE == 2 ~ "African American",
RACE == 3 ~ "White",
RACE == 4 ~ "Other")),
Above200 = as.factor(if_else(above200 == 1, "Above 200 cell count", "Below 200 cell count")),
Randgrp = as.factor(case_when(RANDGRP == 1 ~ "PI + NRTI",
RANDGRP == 2 ~ "NNRTI + NRTI",
RANDGRP == 3 ~ "PI + NNRTI + NRTI")),
twoclass = as.factor(twoclass),
TwoClass = as.factor(
fct_relevel(case_when(twoclass == 1 ~ "Two Class",
twoclass == 2 ~ "Three Class"),"Two Class")), ## adjust Two Class as reference level
Unit = relevel(as.factor(Above200), ref = "Below 200 cell count")) # make UNIT into factor
# Chunk 1
#| code-fold: true
#| code-summary: "Click here to view loaded libraries"
#| message: false
#| warning: false
library(tidyverse)
library(survival)
library(ggfortify) ## to calculate "Schoenfeld residuals vs time"
library(survminer)
library(multcomp)
library(emmeans) ## to calculate hazard ratios
library(gtsummary) ## to make table
library(flextable) ## export tables to Word
# Chunk 2
f6 <- read.csv("first6.csv")
# Chunk 3
#| code-fold: true
#| code-summary: "Click here to view data cleaning code"
f6 <- f6 %>%
mutate(Gender = as.factor(if_else(GENDER == 1, "Male", "Female", missing = NULL)),
Race = as.factor(case_when(RACE == 1 ~ "Latino",
RACE == 2 ~ "African American",
RACE == 3 ~ "White",
RACE == 4 ~ "Other")),
Above200 = as.factor(if_else(above200 == 1, "Above 200 cell count", "Below 200 cell count")),
Randgrp = as.factor(case_when(RANDGRP == 1 ~ "PI + NRTI",
RANDGRP == 2 ~ "NNRTI + NRTI",
RANDGRP == 3 ~ "PI + NNRTI + NRTI")),
twoclass = as.factor(twoclass),
TwoClass = as.factor(
fct_relevel(case_when(twoclass == 1 ~ "Two Class",
twoclass == 2 ~ "Three Class"),"Two Class")), ## adjust Two Class as reference level
Unit = relevel(as.factor(Above200), ref = "Below 200 cell count")) # make UNIT into factor
# Chunk 4: tbl-study-pop
#| label: tbl-study-pop
#| tbl-cap: "Study Population Characteristics"
#| tbl-cap-location: top
t1 <- f6 %>%
dplyr::select(AGE, Gender, Race, Above200, Randgrp) %>%
tbl_summary(by = Randgrp,
label = list("AGE" = "Age",
"Above200" = "Cell Count")) %>%
add_overall() %>%
as_flex_table()
t1
# Chunk 6: km model
AIDS_fit <- survfit(Surv(t2qoi, qoi) ~ TwoClass, data = f6)
# Chunk 7: fig-kmplot
#| label: fig-kmplot
#| fig-cap: "Kaplan-Meier: AIDS Survival Analysis"
#| warning: false
autoplot(AIDS_fit) +
xlab("Time (Months)") +
ylab("AIDS Survival Percentage") +
labs(title = "Time to AIDS",
subtitle = "By Treatment Type") +
theme_minimal()
# Chunk 8
AIDS_model <- coxph(Surv(t2qoi, qoi) ~ TwoClass + Unit + Above200, data = f6)
summary(AIDS_model)
# Chunk 10: tbl-AIDS
#| label: tbl-AIDS
#| tbl-cap: "Multiple Cox Proportional Hazards Regression"
#| tbl-cap-location: top
AIDS_table <- tbl_regression(AIDS_model, exponentiate = TRUE) %>%
bold_labels() %>%
italicize_levels() %>%
as_flex_table()  ## this is so I can copy-paste directly from the html render
AIDS_table ## turns out I can copy-paste directly from the html render!!!
# Chunk 11
AIDS_model_unadj <- coxph(Surv(t2qoi, qoi) ~ TwoClass, data = f6)
summary(AIDS_model_unadj)
# Chunk 12: fig-schoenfeld
#| label: fig-schoenfeld
#| fig-cap: " Schoenfeld Residuals vs Time Plot"
#| fig-cap-location: top
AIDS_resid <- cox.zph(AIDS_model_unadj)
ggcoxzph(AIDS_resid)
AIDS_model <- coxph(Surv(t2qoi, qoi) ~ TwoClass + Unit + Above200, data = f6)
summary(AIDS_model)
AIDS_model <- coxph(Surv(t2qoi, qoi) ~ TwoClass + Unit + Above200, data = f6)
summary(AIDS_model)
#| label: tbl-AIDS
#| tbl-cap: "Multiple Cox Proportional Hazards Regression"
#| tbl-cap-location: top
AIDS_table <- tbl_regression(AIDS_model, exponentiate = TRUE) %>%
bold_labels() %>%
italicize_levels() %>%
as_flex_table()  ## this is so I can copy-paste directly from the html render
AIDS_table ## turns out I can copy-paste directly from the html render!!!
